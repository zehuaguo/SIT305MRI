<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mri">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RestaurantDetail"></activity>
        <activity android:name=".RestaurantList" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Others.OthersActivity" />
        <activity android:name=".Dessert.DessertActivity" />
        <activity android:name=".Junk.JunkActivity" />
        <activity android:name=".Asian.AsianActivity" />
        <activity android:name=".Western.WesternActivity" />
        <activity android:name=".Japanese.JapaneseActivity" />
        <activity android:name=".Chinese.ChineseActivity" />
        <activity android:name=".Korean.KoreanActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>